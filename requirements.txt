# requirements.txt — Herramienta de Pesado de Service Bulletins (main.py)
# Compatible con Python 3.9–3.13 en Windows.
#
# NOTA: tkinter NO se instala con pip. Viene incluido con Python
# (con el instalador oficial de python.org ya está disponible).
# Tampoco hace falta pywin32: el diálogo nativo de carpeta usa ctypes.

# ---------------------------------------------------------------------------
# Dependencias de EJECUCIÓN (las que necesita la app para funcionar)
# ---------------------------------------------------------------------------
pandas>=2.0          # DataFrames y escritura del informe Excel
openpyxl>=3.1        # motor de pandas para escribir .xlsx (engine="openpyxl")
tabulate>=0.9        # usado en metodosrecuento.py

# ---------------------------------------------------------------------------
# Dependencia de COMPILACIÓN (solo para generar el .exe con main.spec)
# ---------------------------------------------------------------------------
pyinstaller>=6.6     # formato de spec 6.x; soporta numpy 2.x y Python recientes

# numpy lo instala pandas automáticamente con la versión adecuada para tu Python;
# no hace falta fijarlo aquí. Si alguna compilación de PyInstaller fallara por
# numpy, actualiza pyinstaller a la última versión.
